name: reminders-processing
engine: ~1
type: cron
ttl: 270
cron:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
command:
  - "python"
  - "-m"
  - "jobs.send_reminders"
image:
  repo: covidflow-action-server
