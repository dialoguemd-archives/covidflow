from distutils.core import setup

setup(
    name="action-server",
    description="Rasa Action Server for covidflow",
    version="0.0.0",
    author="nuecho",
    packages=[],
    install_requires=[],
)
