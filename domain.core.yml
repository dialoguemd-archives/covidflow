intents:
  - greet
  - goodbye
  - affirm
  - deny
  - checkin_return
  - suspect
  - tested_positive

actions:
  - action_cheer_up

session_config:
  session_expiration_time: 60 # value in minutes
  carry_over_slots_to_new_session: true
