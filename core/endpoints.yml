action_endpoint:
  url: http://action-server:8080/webhook
tracker_store:
  type: SQL
  dialect: postgresql
  url: ${SQL_TRACKER_STORE_URL}
  db: ${SQL_TRACKER_STORE_DB}
  username: ${SQL_TRACKER_STORE_USER}
  password: ${SQL_TRACKER_STORE_PASSWORD}
